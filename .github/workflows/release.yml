name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - name: Setup mise
        uses: jdx/mise-action@v4
        with:
          install: true
          cache: true

      - name: Build
        run: mise run build:solution

      - name: Run Tests
        run: mise run test

  build:
    needs: test
    permissions:
      contents: read
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            rid: linux-x64
          - os: ubuntu-24.04-arm
            rid: linux-arm64
          - os: windows-latest
            rid: win-x64
          - os: macos-15-intel
            rid: osx-x64
          - os: macos-14
            rid: osx-arm64

    steps:
      - uses: actions/checkout@v7

      - name: Setup mise
        uses: jdx/mise-action@v4
        with:
          install: true
          cache: true

      - name: Build
        run: mise x -- dotnet publish -c Release -r ${{ matrix.rid }}

      - name: Package
        id: package
        shell: pwsh
        run: |
          $TARGET = "${{ matrix.rid }}".StartsWith("win") ? "fgvm.exe" : "fgvm"
          $SOURCE = Join-Path "Fgvm.Cli" "bin" "Release" "net10.0" "${{ matrix.rid }}" "publish" "$TARGET"
          $package = ./scripts/package-release.ps1 -Rid "${{ matrix.rid }}" -Source $SOURCE
          "artifact_name=$($package.ArtifactName)" >> $env:GITHUB_OUTPUT

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: fgvm-${{ matrix.rid }}
          path: |
            ${{ steps.package.outputs.artifact_name }}
            ${{ steps.package.outputs.artifact_name }}.sha256
          if-no-files-found: error

  # Runs after the binaries so nothing is published until every artifact has built.
  containers:
    needs: [test, build]
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/container.yml

  release:
    needs: [build, containers]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # Scoped: a called workflow shares this run, so container.yml's digest artifacts land here too.
      - uses: actions/download-artifact@v8
        with:
          pattern: fgvm-*

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            */fgvm-*.zip
            */fgvm-*.tar.gz
            */fgvm-*.sha256
          generate_release_notes: false
