name: Integration
permissions:
  contents: read

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    types:
      - auto_merge_enabled
  merge_group:
    types: [checks_requested]

jobs:
  test:
    name: Integration (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, ubuntu-24.04-arm, windows-latest, macos-14]
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Build
        run: dotnet build --configuration Release

      - name: Run Unit Tests
        run: dotnet test Fgvm.Tests --configuration Release --no-build --verbosity normal --logger "console;verbosity=detailed"

      - name: Prepare Integration Fixtures
        run: |
          dotnet run Fgvm.Tests.Integration/Fixtures/BuildFixtures.cs
          dotnet run Fgvm.Tests.Integration/Fixtures/PublishCli.cs

      - name: Run Integration Tests
        run: dotnet test Fgvm.Tests.Integration --configuration Release --no-build --verbosity normal --logger "console;verbosity=detailed"

  required:
    name: Integration
    runs-on: ubuntu-24.04
    needs: test
    if: ${{ always() }}
    steps:
      - name: Verify Integration Matrix Result
        run: |
          if [ "${{ needs.test.result }}" != "success" ]; then
            echo "::error::Integration matrix finished with result '${{ needs.test.result }}'."
            exit 1
          fi
